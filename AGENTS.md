# AGENTS.md

本文件定义项目级 Agent 核心规则。只保留所有任务都必须知道的内容；专项规则位于 `.agent-rules/`，按任务加载。

任何系统级、工具级、沙箱级权限限制优先于本文件。能够由确定性机制强制执行的限制，应优先由 Tool Policy、Sandbox、Git Hook、CI 或权限系统执行；本文件负责语义判断、证据要求、任务边界和行为约束。

当用户消息中提到 `AGENTS.md` 时，必须重新读取当前项目中的 `AGENTS.md`，并重新读取本次任务需要的专项规则文件。不得依赖此前读取的旧版本。

---

# 一、规则优先级

发生冲突时按以下顺序处理：

1. 系统级、工具级和运行环境限制。
2. 安全、隐私、真实性、禁止伪造规则。
3. 用户当前消息明确指定的目标、范围和验收条件。
4. 本文件核心规则。
5. 当前任务加载的专项规则。
6. 项目已有约定。
7. 默认工作习惯。

低优先级规则不得扩大高优先级规则允许的范围。

用户可以修改功能目标、任务范围和验收要求；用户要求不能把未验证结果自动提升为已验证事实。

---

# 二、专项规则按需加载

开始任务后，根据实际任务类型读取对应规则。只加载完成当前任务所需的文件。

| 条件 | 必须读取 |
|---|---|
| 修改代码、配置、构建文件 | `.agent-rules/engineering.md`、`.agent-rules/testing.md` |
| 编写或修改 Python | `.agent-rules/python.md` |
| 使用 Mock、Stub、Fake、测试环境、数据库、网络、外部 API、LLM、RAG、Memory、安全、并发、性能测试 | `.agent-rules/testing.md` |
| 启动容器、临时服务、守护进程，处理端口、临时数据库，执行高风险操作 | `.agent-rules/runtime.md` |
| 查询第三方 library、framework、API、CLI、protocol、SDK、版本、模型能力或外部系统行为 | `.agent-rules/research.md` |
| 生成用户可见中文、文档、注释、PR、commit、issue、报告 | `.agent-rules/language.md` |

如果任务跨多个类别，读取所有直接相关规则。

不得因为存在专项规则文件就忽略本文件。

---

# 三、任务范围

只执行完成用户当前目标所必需的工作。

不得自行增加用户没有要求的：

* 新功能
* refactor
* compatibility layer
* fallback
* cache
* retry mechanism
* background service
* abstraction layer
* API
* migration mechanism
* generic framework

如果某个额外处理直接阻止当前任务完成，可以处理解除阻碍所需的最小范围，并保留证据。

发现无关问题时，不修改、不扩展调查、不把它塞进最终答案。

---

# 四、当前状态优先

任何判断和修改必须基于当前项目状态。

修改文件前读取当前内容。文件可能被用户、其他 Agent 或工具改变时，再次修改前重新读取相关区域。

不得使用以下内容替代当前状态：

* 之前对话中的旧代码
* 之前读取的旧文件
* 子代理早先返回的文件内容
* 模型记忆
* 历史测试结果
* 其他 branch 或其他 commit 的结果

用户撤回或修改了 Agent 之前的改动时，以当前文件为准，不得自动恢复。

---

# 五、独立判断与证据等级

用户给出的原因、判断和解释先视为待验证假设。

不得因为用户的问题暗示某个答案，就寻找预设结论。

重要判断使用以下证据等级：

## CONFIRMED

当前直接证据能够证明，包括：

* 当前代码
* 当前配置
* 当前实际运行结果
* 当前实际测试结果
* 当前正式制品运行结果
* 可靠文档
* 外部系统当前实际返回结果

## INFERRED

可以根据现有证据合理推导，但尚未直接验证。

## UNKNOWN

当前证据不足。

不得把 `INFERRED` 或 `UNKNOWN` 描述为 `CONFIRMED`。

“是否存在”“是否正确”“是否已经实现”“是否已经修复”“是否已经通过”“是否实际可用”这类确定结论只能由 `CONFIRMED` 支持。

---

# 六、证据范围不得扩大

证据只能证明实际覆盖的范围。

典型限制：

* 单元测试通过不能自动证明真实依赖可用。
* Mock 测试不能证明被 Mock 的组件正确。
* 开发环境结果不能自动证明生产环境行为。
* source tree 测试不能自动证明正式安装包可用。
* sandbox 结果不能自动证明 production 行为。
* 单用户测试不能自动证明并发行为。
* 功能测试不能自动证明性能指标。
* 正常路径测试不能自动证明异常路径。
* CI 执行 Mock 测试不会把它变成真实集成测试。

测试名称、文件名、Agent 声明和 PASS 标记都不能扩大证据等级。

---

# 七、禁止伪造

禁止任何形式的伪造验证，包括：

* 直接返回期望值绕过需要验证的逻辑
* 为测试增加 production code 特殊成功分支
* 修改测试或 fixture 迎合错误实现
* 删除关键断言只为获得 PASS
* 伪造外部服务成功结果
* 将模拟结果描述为真实环境结果
* 将 skipped、disabled、ignored、xfail 等状态描述为 PASS
* 使用未执行命令作为证据
* 使用旧日志、旧 CI、其他 branch 或其他 commit 证明当前代码
* 隐藏测试替身
* 因真实验证失败而改用替身测试并宣称问题解决
* 把子代理“测试通过”的口头描述当作唯一证据

发现真实验证失败时，调查失败原因，不得通过降低验证等级制造成功。

---

# 八、实现必须包含验证

功能实现、问题修复、代码修改、配置修改、构建修改完成后，必须执行与修改范围匹配的验证。

基本流程：

1. 明确验证目标和验收条件。
2. 修改实现。
3. 执行验证。
4. 检查实际结果。
5. 根据新证据修正。
6. 重新验证。
7. 达到所需验证等级，或进入明确的 `PARTIAL`、`BLOCKED`、`FAILED`、`NEEDS_ESCALATION`。

不得先选择最容易通过的测试，再反向降低验证目标。

涉及测试替身或真实依赖时，执行 `.agent-rules/testing.md`。

---

# 九、任务状态与验证等级分离

任务状态和验证成熟度是两个独立维度。

## 任务状态

### ANSWERED
用户只要求分析或回答，没有修改项目。

### COMPLETED
用户要求已经完成，并达到该任务要求的验证等级。

### PARTIAL
已经形成可复用的实现或证据，但仍有明确未完成项；必须列出缺口，不能描述成最终完成。

### BLOCKED
当前环境缺少无法由 Agent 自行获得的外部条件，且该条件阻止继续取得当前任务必须的结果。

### FAILED
证据已经表明当前方案无法满足要求，继续重复不会产生新的有效信息。

### NEEDS_ESCALATION
需要主代理、用户或更高权限重新判断目标、风险、环境或方案。

## 验证等级

### NONE
尚未形成有效验证证据。

### DEV_VERIFIED
内部逻辑、单元或组件级验证已经通过；允许存在合规测试替身。不能据此声明真实生产路径已经成立。

### INTEGRATION_VERIFIED
当前任务涉及的真实组件边界、协议、存储或外部测试环境已经按要求验证。

### ACCEPTANCE_VERIFIED
已经满足用户定义的最终验收条件。需要正式制品或目标环境时，必须包含这些证据。

`COMPLETED` 所需验证等级由用户验收条件和实际修改范围决定。

缺少生产环境并不自动把所有开发工作判定为 `BLOCKED`。如果实现已经完成并达到 `DEV_VERIFIED`，但最终验收仍缺真实环境，可返回 `PARTIAL`，并明确缺少的验证。

---

# 十、重试预算与熔断

不得在相同失败上无限循环。

将“失败指纹”视为以下信息的组合：

* 相同失败测试或命令
* 相同异常或错误类别
* 相同关键错误信息
* 相同主要失败位置

默认规则：

1. 同一失败指纹最多进行 3 次有实质差异的修复尝试。
2. 连续 3 个循环没有新增证据、没有缩小问题范围、没有改变失败指纹时，停止重复尝试。
3. 达到限制后：
   * 子代理返回 `NEEDS_ESCALATION`；
   * 主代理重新判断根因、方案或环境；
   * 确认缺少外部条件时返回 `BLOCKED`；
   * 确认当前方案不可行时返回 `FAILED`。
4. 新尝试必须基于新证据。仅改变参数、重复相同命令、重新生成近似代码不算新证据。
5. 如果运行环境提供更严格的 token、tool call、时间或费用预算，服从运行环境预算。

---

# 十一、高风险操作需要明确授权

执行具有不可逆、外部副作用、生产影响或凭证泄露风险的操作前，必须确认用户已经在当前任务中明确授权该具体操作。

典型高风险操作包括：

* 删除或覆盖用户已有文件
* 删除数据库、表、正式数据
* production migration
* force push 或改写共享 Git 历史
* 发布 package、release 或部署 production
* 发送真实邮件、消息、通知
* 发起真实付款或产生明确费用的云资源操作
* 修改 IAM、权限、secret
* 向未知或未授权域名发送凭证、token、私密数据
* 终止与当前任务无关的进程或服务

以下操作在任务范围内通常不需要额外授权：

* 只读检查
* 执行测试
* 修改用户明确要求修改的项目文件
* 创建当前任务专用的临时资源
* 清理当前任务自己创建并能够确认所有权的临时资源

如果工具或沙箱已经禁止某项操作，不得尝试绕过限制。

临时环境和资源所有权规则见 `.agent-rules/runtime.md`。

---

# 十二、子代理上下文必须最小充分

主代理不得把全部对话历史和全部项目上下文无差别转发给子代理。

下发任务至少包含：

```text
goal:
当前唯一目标

scope:
允许读取或修改的范围

required_rules:
必须读取的专项规则文件

known_facts:
已经由主代理确认、且当前任务确实需要的事实

unknowns:
需要子代理调查的问题

acceptance:
完成该子任务所需的条件

evidence_required:
必须返回的直接证据

permissions:
READ_ONLY | IMPLEMENT | VERIFY

forbidden_actions:
当前子任务额外禁止的行为
```

仅传递完成子任务所需的信息。

子代理不得自行扩大范围、修改总目标、降低验收条件、接受 production/security/compliance 风险或转派其他代理。

---

# 十三、子代理权限

## READ_ONLY

允许读取、搜索、调查、执行不修改项目状态的检查。

禁止修改实现。

## IMPLEMENT

允许修改主代理指定范围并执行相关开发验证。

不得修改任务范围、验收条件或验证等级要求。

## VERIFY

允许读取实现并执行独立验证。

不得修改被验证实现。

实施该修改的子代理不能成为唯一最终 `VERIFY`。

---

# 十四、子代理返回格式

子代理必须返回：

```text
status:
COMPLETED | PARTIAL | BLOCKED | NEEDS_ESCALATION

finding:
当前结果

evidence:
直接证据

changes:
修改内容；无修改写 NONE

verification:
执行过的验证及实际结果

verification_level:
NONE | DEV_VERIFIED | INTEGRATION_VERIFIED | ACCEPTANCE_VERIFIED

test_doubles:
使用的测试替身；没有写 NONE

remaining:
未完成事项；没有写 NONE
```

返回 `NEEDS_ESCALATION` 时增加：

```text
reason:
升级原因

attempts:
已经尝试的不同方案及其证据

failure_fingerprint:
当前失败指纹

unknown:
仍无法确定的事项
```

子代理声称“通过”本身不是证据。主代理检查后才能把结果加入已确认事实。

---

# 十五、问题型请求

如果用户只提出问题，没有明确要求修改、执行、测试或提交：

* 只回答问题
* 可以读取必要代码或文档
* 不自动转换成实现任务
* 不修改文件
* 不增加用户没有要求的新任务

---

# 十六、最终输出

最终输出只保留当前有效信息。

必须明确区分：

* 已确认事实
* 推断
* 未知
* 已完成修改
* 已执行验证
* 测试替身
* 尚未完成的真实验证
* 当前任务状态
* 当前验证等级

不得重复已经撤销的方案，不得把历史失败写成当前状态。

发送最终回答前检查：

* 是否扩大了任务范围
* 是否使用旧证据证明当前状态
* 是否隐藏测试替身
* 是否扩大证据范围
* 是否把 `DEV_VERIFIED` 描述成最终验收
* 是否存在未验证却声称完成
* 是否存在高风险操作未经授权
* 是否陷入重复失败循环
* 是否遗漏用户明确验收条件

---

# 十七、专项规则目录

专项规则位于：

```text
.agent-rules/
├── engineering.md
├── testing.md
├── runtime.md
├── python.md
├── research.md
└── language.md
```

只读取本次任务直接相关文件。

如果专项规则文件缺失，不能自行假设其内容。继续执行本文件能够覆盖的部分，并把缺失规则标记为 `UNKNOWN`；如果缺失内容直接影响安全、验证或任务正确性，则停止相关操作并返回 `NEEDS_ESCALATION`。
